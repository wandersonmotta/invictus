/**
 * Mapbox premium style config (token is publishable).
 *
 * When you have it, paste here:
 * - token: your Mapbox public token (pk...)
 * - style: either `mapbox://styles/<user>/<styleId>` OR `<user>/<styleId>`
 */
export const MAPBOX_PREMIUM = {
  token: "",
  style: "",
} as const;
