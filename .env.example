# CONFIGURAÇÕES DO SUPABASE (Essenciais para o Site funcionar)
VITE_SUPABASE_URL="https://krtjexfyixnhjehndyop.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sua_chave_publica_aqui"

# CONFIGURAÇÕES DE RECUPERAÇÃO ( Necessário para npm run restaurar )
# Essa chave é usada para scripts rodarem como admin localmente.
# Pode ser encontrada nas configurações do projeto Supabase -> API -> service_role secret
SUPABASE_SERVICE_KEY="sua_chave_service_role_aqui"

# CONFIGURAÇÕES DO STRIPE ( Para restaurar Joyce e Thiago )
# Chave secreta do Stripe (começa com sk_live_...)
STRIPE_SECRET_KEY="sk_live_..."

# --- NOTA SOBRE EDGE FUNCTIONS (Reset de Senha / Emails) ---
# As chaves abaixo NÃO ficam neste arquivo .env local se você estiver usando o projeto hospedado.
# Elas devem ser adicionadas no Painel do Supabase em: Settings -> Edge Functions -> Secrets.
#
# RESEND_API_KEY="re_..." (Chave do Resend para enviar emails)
# RESEND_FROM_EMAIL="recuperaracesso@invictusfraternidade.com.br" (Email remetente verificado no Resend)
# ADMIN_EMAIL_ALLOWLIST="seu-email@exemplo.com" (Para liberar admin automaticamente)
# CPFCNPJ_TOKEN="..." (Se usado para validação)
GOOGLE_AI_STUDIO_API_KEY="sua_chave_gemini_aqui"
